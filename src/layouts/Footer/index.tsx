import { MainFooter, TextFooter } from "./styles";

export const Footer: React.FC = ({}) => {
  return (
    <MainFooter>
      <TextFooter>MKS sistemas © Todos os direitos reservados</TextFooter>
    </MainFooter>
  );
};
